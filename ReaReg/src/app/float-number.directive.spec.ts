import {FloatNumberDirective} from './float-number.directive';

describe('FloatNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new FloatNumberDirective();
    expect(directive).toBeTruthy();
  });
});
